#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    vector<ll> h(n+1, 0);
    for(int i = 1; i <= n; i++){
        cin >> h[i];
    }
    ll idx = 0;
    while(idx <= n){

    }
    
    return 0;
}